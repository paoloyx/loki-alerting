groups:
  - name: too_much_errors
    rules:
      - alert: TooMuchErrors
        expr: |
          sum(rate({namespace="default"} |= "error" [5m])) by (job) > 1
        for: 1m
        labels:
            severity: critical
        annotations:
            summary: Too much errors during last minute